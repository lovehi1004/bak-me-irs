<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gov.me.irs.biz.aply.mapper.BizScsnChgAplyMapper">
	
	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 권리의무승계목록조회 총건수 조회 -->
	<select id="selectScsnChgAplyListCnt" resultType="int" parameterType="map">
		/* BizScsnChgAplyMapper.selectScsnChgAplyListCnt */
		SELECT COUNT(*)
		  FROM irsuser.TEMP_TABLE
		 WHERE 1 = 1
	</select>
	
	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 권리의무승계목록조회 -->
	<select id="selectScsnChgAplyList" resultType="egovMap" parameterType="map">
		<include refid="gov.me.irs.common.PagingMapper.pagingPreSql" />
		/* BizScsnChgAplyMapper.selectScsnChgAplyList */
		SELECT '컬럼정보' AS COL
		  FROM irsuser.TEMP_TABLE
		 WHERE 1 = 1
		 ORDER BY TEMP_SEQ ASC
		<include refid="gov.me.irs.common.PagingMapper.pagingPostSql" />
	</select>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 권리의무승계상세조회 -->
	<select id="selectScsnChgAplyDetail" resultType="egovMap" parameterType="map">
		/* BizScsnChgAplyMapper.selectScsnChgAplyDetail */
		SELECT '컬럼정보' AS COL
		  FROM irsuser.TEMP_TABLE
		 WHERE CONDITION = #{condition}
	</select>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 사업공통조회 -->
	<select id="selectBizSrhList" resultType="egovMap" parameterType="map">
		/* BizScsnChgAplyMapper.selectBizSrhList */
		SELECT '컬럼정보' AS COL
		  FROM irsuser.TEMP_TABLE
		 WHERE 1 = 1
		 ORDER BY TEMP_SEQ ASC
	</select>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 권리의무승계등록 -->
	<insert id="insertScsnChgAply" parameterType="map">
		/* BizScsnChgAplyMapper.insertScsnChgAply */
		INSERT
		  INTO irsuser.TEMP_TABLE
			 ( COL
			 , USE_YN
			 , RGTR_ID
			 )
		VALUES
			 ( #{col}
			 , #{useYn}
			 , #{sessionUserId}
			 )
	</insert>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 권리의무승계수정 -->
	<update id="updateScsnChgAply" parameterType="map">
		/* BizScsnChgAplyMapper.updateScsnChgAply */
		UPDATE irsuser.TEMP_TABLE
		   SET COL = #{col}
		     , MDFR_ID = #{sessionUserId}
		     , MDFCN_DT = CURRENT_TIMESTAMP
		 WHERE CONDITION = #{condition}
	</update>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 사업수행자공통조회 -->
	<select id="selectCorpSrhList" resultType="egovMap" parameterType="map">
		/* BizScsnChgAplyMapper.selectCorpSrhList */
		SELECT '컬럼정보' AS COL
		  FROM irsuser.TEMP_TABLE
		 WHERE 1 = 1
		 ORDER BY TEMP_SEQ ASC
	</select>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 사업수행자동의확인 -->
	<update id="updateScsnChgAplyAgree" parameterType="map">
		/* BizScsnChgAplyMapper.updateScsnChgAplyAgree */
		UPDATE irsuser.TEMP_TABLE
		   SET COL = #{col}
		     , MDFR_ID = #{sessionUserId}
		     , MDFCN_DT = CURRENT_TIMESTAMP
		 WHERE CONDITION = #{condition}
	</update>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 사업분야공통조회 -->
	<select id="selectBizSectSrhList" resultType="egovMap" parameterType="map">
		/* BizScsnChgAplyMapper.selectBizSectSrhList */
		SELECT '컬럼정보' AS COL
		  FROM irsuser.TEMP_TABLE
		 WHERE 1 = 1
		 ORDER BY TEMP_SEQ ASC
	</select>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 평가결과조회 -->
	<select id="selectScsnChgAplyEvlRslt" resultType="egovMap" parameterType="map">
		/* BizScsnChgAplyMapper.selectScsnChgAplyEvlRslt */
		SELECT '컬럼정보' AS COL
		  FROM irsuser.TEMP_TABLE
		 WHERE CONDITION = #{condition}
	</select>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 문서보완등록 -->
	<insert id="insertScsnChgAplyDocSplmnt" parameterType="map">
		/* BizScsnChgAplyMapper.insertScsnChgAplyDocSplmnt */
		INSERT
		  INTO irsuser.TEMP_TABLE
			 ( COL
			 , USE_YN
			 , RGTR_ID
			 )
		VALUES
			 ( #{col}
			 , #{useYn}
			 , #{sessionUserId}
			 )
	</insert>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 사전심의결과조회 -->
	<select id="selectScsnChgAplyDicDlbrRslt" resultType="egovMap" parameterType="map">
		/* BizScsnChgAplyMapper.selectScsnChgAplyDicDlbrRslt */
		SELECT '컬럼정보' AS COL
		  FROM irsuser.TEMP_TABLE
		 WHERE CONDITION = #{condition}
	</select>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 이의신청등록 -->
	<insert id="insertScsnChgAplyObjcAply" parameterType="map">
		/* BizScsnChgAplyMapper.insertScsnChgAplyObjcAply */
		INSERT
		  INTO irsuser.TEMP_TABLE
			 ( COL
			 , USE_YN
			 , RGTR_ID
			 )
		VALUES
			 ( #{col}
			 , #{useYn}
			 , #{sessionUserId}
			 )
	</insert>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 이의신청조회 -->
	<select id="selectScsnChgAplyObjcAply" resultType="egovMap" parameterType="map">
		/* BizScsnChgAplyMapper.selectScsnChgAplyObjcAply */
		SELECT '컬럼정보' AS COL
		  FROM irsuser.TEMP_TABLE
		 WHERE CONDITION = #{condition}
	</select>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 심의결과조회 -->
	<select id="selectScsnChgAplyDlbrRslt" resultType="egovMap" parameterType="map">
		/* BizScsnChgAplyMapper.selectScsnChgAplyDlbrRslt */
		SELECT '컬럼정보' AS COL
		  FROM irsuser.TEMP_TABLE
		 WHERE CONDITION = #{condition}
	</select>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 추가자료요청조회 -->
	<select id="selectScsnChgAplyDataDmnd" resultType="egovMap" parameterType="map">
		/* BizScsnChgAplyMapper.selectScsnChgAplyDataDmnd */
		SELECT '컬럼정보' AS COL
		  FROM irsuser.TEMP_TABLE
		 WHERE CONDITION = #{condition}
	</select>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 추가자료등록 -->
	<insert id="insertScsnChgAplyDataReg" parameterType="map">
		/* BizScsnChgAplyMapper.insertScsnChgAplyDataReg */
		INSERT
		  INTO irsuser.TEMP_TABLE
			 ( COL
			 , USE_YN
			 , RGTR_ID
			 )
		VALUES
			 ( #{col}
			 , #{useYn}
			 , #{sessionUserId}
			 )
	</insert>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 추가자료확인조회 -->
	<select id="selectScsnChgAplyDataIdnty" resultType="egovMap" parameterType="map">
		/* BizScsnChgAplyMapper.selectScsnChgAplyDataIdnty */
		SELECT '컬럼정보' AS COL
		  FROM irsuser.TEMP_TABLE
		 WHERE CONDITION = #{condition}
	</select>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 추가자료결과조회 -->
	<select id="selectScsnChgAplyDataRslt" resultType="egovMap" parameterType="map">
		/* BizScsnChgAplyMapper.selectScsnChgAplyDataRslt */
		SELECT '컬럼정보' AS COL
		  FROM irsuser.TEMP_TABLE
		 WHERE CONDITION = #{condition}
	</select>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 이력목록조회 -->
	<select id="selectScsnChgAplyHistList" resultType="egovMap" parameterType="map">
		/* BizScsnChgAplyMapper.selectScsnChgAplyHistList */
		SELECT '컬럼정보' AS COL
		  FROM irsuser.TEMP_TABLE
		 WHERE 1 = 1
		 ORDER BY TEMP_SEQ ASC
	</select>

	<!-- [사용자] 사업관리 > 사업 신청 관리 > 권리의무 승계 신청 - 보고서출력 -->
	<select id="selectScsnChgAplyRpt" resultType="egovMap" parameterType="map">
		/* BizScsnChgAplyMapper.selectScsnChgAplyRpt */
		SELECT '컬럼정보' AS COL
		  FROM irsuser.TEMP_TABLE
		 WHERE CONDITION = #{condition}
	</select>


	
</mapper>
